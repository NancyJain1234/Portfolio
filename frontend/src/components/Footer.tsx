const Footer = () => {
  return (
    <footer className="border-t border-white/10 bg-slate-950 px-6 py-6 text-center text-sm text-slate-500">
      Built with React + TypeScript + Tailwind. Architecture ready for AWS Amplify + FastAPI.
    </footer>
  );
};

export default Footer;
